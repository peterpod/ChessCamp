<h2> <%= "#{current_user.username.capitalize.pluralize} Dashboard"%></h2>
<h3><%= status %> Camps  
<% if can? :create, Camp %>
      <%= link_to "Add a new camp", new_camp_path, class: "button tiny radius" %>
    <% end %>
</h3>

<table class="table">
  <tr>
    <th width="200">Name</th>
    <th width="120">Start On</th>
    <th width="120">Time</th>
    <% if current_user.role == "instructor" %>
      <th width="120">Registered Students</th>
    <% end %>
  </tr>

<% @upcoming_camps.each do |camp| %>
  <tr>
    <td><%= link_to camp.name, camp_path(camp) %></td>
    <td align="center"><%= camp.start_date.strftime('%m/%d/%y') %></td>
    <td align="center"><%= camp.time_slot == "am" ? "Morning" : "Afternoon" %></td>
    <!-- <td align="center"><%= camp.registrations.paid.size * camp.cost%></td> -->
    <% if current_user.role == "instructor" %>
      <td><%= link_to 'Registered Students', registered_students_path(camp.students), class: "button tiny radius" %></td>
    <% end %>
  </tr>
<% end %>
</table>    
<!-- Add the pagination using will_paginate gem -->
<span class="paginate">
<%= will_paginate @upcoming_camps, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
</span>